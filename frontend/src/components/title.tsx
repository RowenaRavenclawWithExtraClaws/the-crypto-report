import { Typography } from "@mui/material";

const Title = () => {
  return (
    <div className="app-title">
      <Typography variant="h4">The Crypto Report</Typography>
      <Typography variant="subtitle1">your daily crypto insight!</Typography>
    </div>
  );
};

export default Title;
